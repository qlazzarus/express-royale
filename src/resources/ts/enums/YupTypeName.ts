enum YupTypeName {
    ARRAY = 'array',
    BOOLEAN = 'boolean',
    DATE = 'date',
    MIXED = 'mixed',
    NUMBER = 'number',
    OBJECT = 'object',
    STRING = 'string',
}

export default YupTypeName;
